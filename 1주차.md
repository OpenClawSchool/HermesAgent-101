# HermesAgent-101 1주차 — 나의 AI 비서와 인사하기

## 차시 개요

- **주제:** 내 생애 첫 AI 에이전트와의 만남 — Hermes Agent 시작하기
- **핵심 키워드:** AI 에이전트, Hermes Agent, CLI 첫 실행, 모델 라우팅, 텔레그램 연동, 자동화 후보 발굴
- **목표:** Hermes Agent를 내 환경에서 직접 실행하고, 첫 대화를 나누며 에이전트 시대의 주인공이 된다.

---

## 강의 목표

Hermes Agent를 통해 나만의 AI 비서를 깨우기 위해 필요한 **설치부터 첫 대화, 자동화 후보 발굴까지**의 전체 구조를 학습하고, 직접 실행한다.

## 학습 목표

- AI 에이전트의 개념과 발전 흐름(GPT → Copilot → Agent)을 이해한다.
- Hermes Agent의 핵심 구성 요소를 이해한다.
- 내 환경(로컬 또는 클라우드)에서 Hermes Agent를 설치하고 첫 실행을 할 수 있다.
- CLI로 첫 대화를 시도하고, 질문형과 작업형 요청의 차이를 느낀다.
- Telegram Bot을 만들고 Hermes Agent와 연결해 메신저로 비서와 대화할 수 있다.
- 내 일상 업무에서 자동화 후보 3가지를 찾는다.

---

## 준비물

> ⚠️ **수업 전에 미리 준비해 주세요!**

| 준비물 | 설명 | 준비 방법 |
| --- | --- | --- |
| **GitHub 계정** | 에이전트 프로젝트를 저장·관리할 공간 | [github.com](https://github.com) 에서 무료 가입 |
| **Hermes Agent 실행 환경** | 에이전트를 실행할 환경 | 강사가 제공하는 서버 또는 로컬 PC |
| **모델 API 키** | AI 모델 호출에 사용 (OpenAI, Anthropic 등) | 수업 시간에 안내 또는 사전 발급 |
| **Telegram 계정** | 메신저 비서 연동에 필요 | [telegram.org](https://telegram.org) 에서 무료 설치 및 가입 |
| **인터넷 연결** | 모든 실습에 필요 | Wi-Fi 또는 유선 연결 |

---

## 주요 내용

### 1. AI 에이전트 시대 이해

#### 💡 AI 에이전트란?

AI 에이전트는 단순히 질문에 답하는 챗봇이 아니라, **스스로 판단하고 행동까지 수행하는 AI 시스템**이다.

| 구분 | 챗봇 (예: ChatGPT) | AI 에이전트 (예: Hermes Agent) |
| --- | --- | --- |
| **하는 일** | 질문에 답변 | 질문에 답변 + 직접 작업 수행 |
| **예시** | "이메일 초안 써줘" → 텍스트 출력 | "이메일 보내줘" → 실제로 이메일 전송 |
| **특징** | 사람이 복사·붙여넣기 해야 함 | 에이전트가 알아서 실행 |

#### AI의 발전 흐름

```
GPT (질문-답변) → Copilot (작업 보조) → Agent (자율 실행)
     2022.11            2023~             2025~
```

- **사람이 AI를 사용하는 시대** → **AI를 고용하는 시대**로 변화
- 전문가 에이전트는 특정 분야의 지식을 갖고, 해당 분야 업무를 자동으로 처리

#### Hermes Agent란?

Hermes Agent는 AI 모델과 사용자의 작업 환경을 이어주는 **오픈소스 AI 에이전트 실행 프레임워크**다. 사용자는 자연어로 목표를 말하고, Hermes Agent는 설정된 모델, 도구, 메모리, 프로젝트 지침을 활용해 일을 진행한다.

| 항목 | 내용 |
| --- | --- |
| **유형** | 오픈소스 AI 에이전트 프레임워크 |
| **라이선스** | MIT (무료, 상업적 사용 가능) |
| **참고** | [WikiDocs Hermes Agent 가이드](https://wikidocs.net/book/19414) |

> **💡 핵심 인사이트:** 앞으로는 "AI를 잘 쓰는 사람"이 아니라, **"자기 분야의 전문가 에이전트를 만든 사람"**이 경쟁력을 가진다.

#### 전문가 에이전트 활용 사례

| 분야 | 에이전트 역할 | 구체적 활용 |
| --- | --- | --- |
| **영어 교육** | 1:1 맞춤형 영어 튜터 | 학생 수준에 맞는 문제 출제, 오답 분석 |
| **마케팅** | 콘텐츠 제작 어시스턴트 | SNS 카피 생성, 트렌드 분석 |
| **부동산** | 매물 분석 비서 | 시세 비교, 투자 수익률 계산 |
| **콘텐츠 제작** | 영상 기획 어시스턴트 | 대본 초안 작성, 업로드 일정 관리 |
| **개발** | 코드 리뷰 보조 | PR 요약, 버그 패턴 탐지, 문서 자동화 |

#### 강사 멘트 예시

> "이제 중요한 것은 AI가 답변을 잘하는가가 아니라, 실제로 일을 처리할 수 있는가입니다. 오늘 수업은 그 출발점인 실행 환경을 만드는 시간입니다."

---

### 2. Hermes Agent 핵심 구조 이해

> **💡 이 섹션의 목적:** 앞에서 직접 실행해 본 Hermes Agent가 내부적으로 어떻게 동작하는지 이해한다. 구조를 알면 에러가 날 때 어디를 확인해야 하는지 알 수 있다.

#### Hermes Agent의 핵심 구성 요소

```
┌──────────────────────────────────────────────────┐
│                  Hermes Agent                    │
│                                                  │
│  ┌────────────┐  ┌──────────────────────────┐   │
│  │  모델 연결  │  │     도구 시스템            │   │
│  │ (클라우드/  │  │ (파일, 터미널, 브라우저)   │   │
│  │  로컬 LLM) │  └────────────┬─────────────┘   │
│  └──────┬─────┘               │                  │
│         │                     │                  │
│         └──────────┬──────────┘                  │
│                    ▼                             │
│          ┌──────────────────┐                   │
│          │   Agent Core     │                   │
│          │  (판단 + 실행)   │                   │
│          └────────┬─────────┘                   │
│                   │                             │
│       ┌───────────┼──────────────┐              │
│       ▼           ▼              ▼              │
│  ┌─────────┐ ┌────────┐  ┌──────────────┐      │
│  │ MEMORY  │ │ SOUL   │  │  AGENTS.md   │      │
│  │ (기억)  │ │ (성격) │  │ (프로젝트    │      │
│  └─────────┘ └────────┘  │  지침)       │      │
│                           └──────────────┘      │
└──────────────────────────────────────────────────┘
```

| 구성 요소 | 역할 | 쉬운 비유 |
| --- | --- | --- |
| **모델 연결** | OpenAI, Anthropic, Ollama 등 외부·로컬 모델 호출 | 에이전트의 두뇌 |
| **도구 시스템** | 파일 읽기/쓰기, 터미널 실행, 웹 브라우징 | 에이전트의 손 |
| **메모리(MEMORY.md)** | 사용자 선호·맥락을 마크다운으로 저장 | 에이전트의 메모장 |
| **성격(SOUL.md)** | 에이전트의 말투, 태도, 동작 방식을 정의 | 에이전트의 성격 |
| **프로젝트 지침(AGENTS.md)** | 프로젝트마다 다른 규칙과 컨텍스트를 제공 | 업무 안내서 |

#### Hermes Agent의 주요 기능

| 기능 | 설명 |
| --- | --- |
| 🖥️ **로컬 실행** | Mac, Windows, Linux에서 자체 호스팅. 데이터가 내 기기에 보관 |
| 🧠 **영구 기억** | 사용자 선호·맥락을 Markdown으로 저장. 모델 변경 후에도 대화 유지 |
| 🌐 **도구 연결** | 파일 읽기/쓰기, 셸 명령, 웹 브라우징 가능 |
| ⚙️ **모델 라우팅** | 요청 성격에 따라 다른 모델이나 설정을 자동 선택 |
| 🔌 **스킬 시스템** | 반복 업무를 재사용 가능한 스킬로 캡슐화 |
| 💬 **메시징 연동** | Telegram, Discord 등 메신저를 통한 비서 연동 (3주차) |

#### Hermes Agent 동작 흐름

```
사용자가 CLI에서 요청 입력
  → Agent Core가 요청을 분석하고 판단
    → 필요 시 도구(파일, 터미널, 웹)를 호출
      → MEMORY, SOUL, AGENTS.md를 참조하여 응답 생성
        → 결과를 사용자에게 출력
```

---

### 3. 설치와 첫 실행

#### 🔨 실습: Hermes Agent 설치 및 첫 실행

> ⚠️ **확인 필요:** 아래 명령어와 설치 흐름은 강의 전에 WikiDocs 최신 문서를 기준으로 재점검한다.

**Step 1. 환경 확인**

| 항목 | 최소 | 권장 |
| --- | --- | --- |
| **OS** | macOS / Linux / Windows (WSL2) | macOS / Ubuntu 22.04 |
| **Node.js** | v18 이상 | 최신 LTS |
| **메모리** | 4GB RAM | 8GB 이상 |
| **네트워크** | 클라우드 API 사용 시 인터넷 연결 | — |

**Step 2. 서버 접속 (강사 제공 환경 사용 시)**

원격 서버에 쉽게 접속하기 위해 무료 SSH 클라이언트인 MobaXterm을 사용한다. (Windows 환경 권장)

1. [MobaXterm 공식 웹사이트](https://mobaxterm.mobatek.net/download.html)에서 **Home Edition**을 다운로드하고 설치한다.
2. MobaXterm을 실행한 뒤, **Session → SSH**를 클릭한다.
3. 강사가 제공한 IP 주소, 사용자명, `key.pem` 파일로 접속한다.

> **💡 TIP:** Mac 사용자라면 기본 터미널에서 바로 접속할 수 있다.
>
> ```bash
> ssh -i key.pem user@[서버IP주소]
> ```

**Step 3. Hermes Agent 설치**

```bash
# WikiDocs 최신 설치 명령 확인 후 여기에 기입
# 예시 (확인 필요):
npm install -g hermes-agent
```

**Step 4. API 키 설정**

```bash
# 환경변수 이름과 설정 방식은 강의 전 최신 문서로 확인한다
export OPENAI_API_KEY="sk-..."
# 또는 로컬 LLM 사용 시 Ollama 등의 설정
```

**Step 5. 첫 실행**

```bash
hermes
```

터미널에 Hermes Agent 프롬프트가 뜨면 성공이다.

---

### 4. CLI 기본기와 첫 대화

#### 자연어로 부탁하기

Hermes Agent는 명령어를 암기할 필요가 없다. 자연어로 요청하면 된다.

```text
Hermes Agent를 처음 배우는 사람에게 오늘 배울 내용을 5줄로 설명해줘.
```

#### 질문형 vs 작업형 요청

| 구분 | 예시 | 결과 |
| --- | --- | --- |
| **질문형** | "Hermes Agent가 뭐야?" | 설명 텍스트 |
| **작업형** | "내 README를 읽고 처음 온 사람이 이해하기 어려운 부분을 알려줘" | 구체적 피드백 산출물 |
| **파일 요청형** | "방금 설명을 강의 노트 형식으로 파일로 저장해줘" | 파일 생성 |

> **포인트:** 답변의 "똑똑함"이 아니라 **요청 형태의 차이**를 관찰한다. 질문은 답변을 만들고, 작업 요청은 산출물을 만든다.

#### 슬래시 명령어 맛보기

슬래시 명령어는 `/`로 시작하는 특수 명령이다. 대화 흐름 안에서 상태 확인이나 설정 변경을 빠르게 할 수 있다.

```text
/help       # 사용 가능한 명령어 목록
/model      # 현재 사용 중인 모델 확인
/memory     # 저장된 메모리 확인
```

> ⚠️ **확인 필요:** 실제 지원되는 슬래시 명령어 목록은 강의 전 최신 문서로 확인한다.

---

### 5. 텔레그램으로 내 비서와 연결하기

CLI 대화에 익숙해졌다면, 이제 스마트폰에서도 비서에게 말을 걸 수 있도록 **Telegram을 연동**한다.  
텔레그램 연동이 완료되면 언제 어디서나 메신저로 Hermes Agent에게 업무를 맡길 수 있다.

#### 전체 연동 구조

```
사용자 (Telegram 앱)
  │  메시지 전송
  ▼
Telegram 서버
  │  Bot API (webhook 또는 polling)
  ▼
Hermes Agent (내 서버 또는 PC)
  │  요청 분석 → 도구 실행
  ▼
Telegram 서버
  │  응답 전송
  ▼
사용자 (Telegram 앱)
```

#### Step 1. Telegram Bot 만들기 (BotFather)

텔레그램 봇은 **BotFather**라는 공식 봇을 통해 만든다. 앱 개발 지식이 없어도 채팅만으로 만들 수 있다.

1. 텔레그램 앱에서 **@BotFather**를 검색해 대화를 시작한다.
2. `/newbot` 명령을 입력한다.
3. 봇 이름을 입력한다. (예: `My Hermes Assistant`)
4. 봇 사용자명을 입력한다. 반드시 `bot`으로 끝나야 한다. (예: `my_hermes_bot`)
5. BotFather가 **Bot Token**을 발급해 준다.

```
예시 토큰 형식:
1234567890:ABCdefGHIjklMNOpqrSTUvwxYZ-abcdefgh
```

> ⚠️ **주의:** Bot Token은 비밀번호와 같다. 절대 공개 저장소(GitHub 등)에 올리지 않는다.

#### Step 2. Bot Token을 환경변수로 설정

```bash
export TELEGRAM_BOT_TOKEN="1234567890:ABCdefGHIjklMNOpqrSTUvwxYZ-abcdefgh"
```

또는 Hermes Agent 설정 파일(`.env` 또는 `config.yaml`)에 기입한다.

```env
# .env 파일 예시 (확인 필요: 실제 키 이름은 최신 문서로 확인)
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrSTUvwxYZ-abcdefgh
```

> ⚠️ **확인 필요:** 실제 환경변수 이름과 설정 파일 위치는 강의 전 WikiDocs 최신 문서로 확인한다.

#### Step 3. Hermes Agent에서 텔레그램 게이트웨이 활성화

```bash
# 텔레그램 연동 옵션으로 실행 (명령어는 확인 필요)
hermes --gateway telegram
```

또는 설정 파일에서 게이트웨이를 활성화한다.

```yaml
# config.yaml 예시 (확인 필요: 실제 키 구조는 최신 문서로 확인)
gateway:
  telegram:
    enabled: true
    token: "${TELEGRAM_BOT_TOKEN}"
```

#### Step 4. 봇과 대화 시작

1. 텔레그램 앱에서 내 봇(@my_hermes_bot)을 검색한다.
2. `/start`를 입력해 대화를 시작한다.
3. 자연어로 요청을 보낸다.

```
사용자: 오늘 할 일 목록 정리해줘
Hermes: 네, 오늘 할 일을 정리했습니다...
```

#### Telegram 연동 시 알아야 할 것

| 항목 | 내용 |
| --- | --- |
| **Polling vs Webhook** | Polling은 봇이 주기적으로 Telegram 서버에 새 메시지를 확인. Webhook은 Telegram이 직접 내 서버로 메시지를 전달. 로컬 개발에서는 Polling이 간단하고, 운영 서버에서는 Webhook이 안정적 |
| **보안** | 봇은 기본적으로 누구나 말을 걸 수 있다. 실습에서는 본인 Chat ID를 허용 목록에 추가하는 방법을 확인한다 |
| **네트워크** | Polling 방식은 서버가 인터넷에 연결되어 있으면 작동. Webhook은 외부에서 접근 가능한 공개 URL(또는 ngrok 같은 터널)이 필요 |

#### 내 Chat ID 확인하기

봇이 나하고만 대화하도록 제한하려면 내 Chat ID를 알아야 한다.

1. 텔레그램에서 **@userinfobot**에 `/start`를 보낸다.
2. 응답으로 나오는 숫자가 내 Chat ID다.
3. Hermes Agent 설정에 허용 Chat ID를 등록한다.

```yaml
# 허용된 사용자만 봇 사용 (설정 키는 확인 필요)
gateway:
  telegram:
    allowed_chat_ids:
      - 123456789
```

---

## 실습

### 🔨 Lab 1. 첫 대화 실행하기

**목표:** Hermes Agent를 실행하고 첫 대화를 기록한다.

**준비물:**
- Hermes Agent 설치 환경
- 모델 API 키 또는 로컬 LLM 설정
- 터미널

**절차:**

1. 터미널을 열고 Hermes Agent를 실행한다.
2. 다음 요청을 입력한다.

    ```text
    Hermes Agent를 처음 배우는 사람에게 오늘 배울 내용을 5줄로 설명해줘.
    ```

3. 답변을 복사해 `week-01-first-chat.md`에 저장한다.
4. 같은 내용을 작업형 요청으로 바꿔 다시 입력한다.

    ```text
    방금 설명을 강의 노트 형식으로 제목, 핵심 개념, 실습, 과제로 정리해줘.
    ```

5. 두 결과를 나란히 놓고 차이를 확인한다.

**성공 기준:**
- Hermes Agent가 대화 입력을 받고 응답한다.
- 일반 질문과 작업형 요청의 결과 차이를 설명할 수 있다.

**막혔을 때:**
- 실행 명령이 최신 문서와 맞는지 확인한다.
- API 키나 로컬 모델 설정이 되었는지 확인한다.
- 네트워크가 필요한 모델을 쓰는 경우 인터넷 연결을 확인한다.
- 설치에 실패한 경우, 강사가 제공하는 샘플 대화 결과를 보고 Lab 2로 넘어간다.

---

### 🔨 Lab 2. 내 업무 자동화 후보 찾기

**목표:** Hermes Agent에게 맡길 수 있는 개인 업무 후보를 발굴한다.

**준비물:**
- 최근 일주일 동안 한 업무 기록 (없으면 기억나는 반복 업무 5개)

**절차:**

1. 최근 일주일 동안 반복해서 한 일을 5개 적는다.
2. 각 항목 옆에 아래 중 하나를 붙인다.

    | 유형 | 예시 |
    | --- | --- |
    | 자료 정리 | 회의록 요약, 이메일 분류 |
    | 문서 작성 | 보고서 초안, README 작성 |
    | 코드 확인 | PR 리뷰, 버그 재현 확인 |
    | 일정 관리 | 미팅 잡기, 할 일 우선순위 정리 |
    | 정보 수집 | 뉴스 요약, 경쟁사 동향 정리 |

3. Hermes Agent에게 다음처럼 요청한다.

    ```text
    아래 반복 업무 중 AI 에이전트로 자동화하기 쉬운 순서대로 정리하고, 이유도 짧게 달아줘.
    ```

4. 결과를 보고 자동화 후보 3개를 고른다.

**성공 기준:**
- 자동화 후보 3개가 나온다.
- 각 후보가 왜 쉬운지 또는 어려운지 한 줄로 설명할 수 있다.
- 각 후보마다 Hermes Agent에게 맡길 첫 요청 문장이 있다.

**막혔을 때:**
- 업무가 떠오르지 않으면 이메일 정리, 회의록 요약, 자료 조사, 코드 확인, 일정 정리 중 하나에서 고른다.
- "입력이 반복되고 출력 형식이 비슷한가"를 기준으로 자동화 가능성을 판단한다.
- 후보가 너무 크면 10분 안에 확인 가능한 크기로 줄인다.

---

### 🔨 Lab 3. 텔레그램으로 Hermes Agent 연결하기

**목표:** Telegram Bot을 만들고 Hermes Agent와 연결해 메신저에서 비서와 대화한다.

**준비물:**
- 텔레그램 앱이 설치된 스마트폰 또는 PC
- Hermes Agent가 실행 중인 환경 (Lab 1 완료 후)
- 인터넷 연결

**절차:**

1. 텔레그램 앱에서 **@BotFather**를 검색하고 `/newbot`을 입력한다.
2. 봇 이름과 사용자명을 설정하고 **Bot Token**을 받는다.
3. Bot Token을 환경변수로 설정한다.

    ```bash
    export TELEGRAM_BOT_TOKEN="여기에_받은_토큰_입력"
    ```

4. Hermes Agent를 텔레그램 게이트웨이 옵션으로 실행한다.

    ```bash
    # 실제 명령은 최신 문서로 확인
    hermes --gateway telegram
    ```

5. **@userinfobot**에서 내 Chat ID를 확인하고 허용 목록에 등록한다.
6. 내 봇을 텔레그램에서 검색해 `/start`를 보낸다.
7. 다음 메시지를 보내 동작을 확인한다.

    ```
    안녕! 오늘 내가 할 일 목록을 정리해줘.
    ```

**성공 기준:**
- 텔레그램 앱에서 메시지를 보내면 Hermes Agent가 응답한다.
- CLI와 텔레그램 두 채널 모두에서 같은 에이전트에게 말을 걸 수 있다.

**막혔을 때:**
- Bot Token이 올바른지 다시 확인한다. (공백이나 따옴표 문제가 많다)
- Hermes Agent가 실행 중인지 터미널에서 확인한다.
- 로컬 환경에서 Webhook이 필요한 경우, 강사의 안내에 따라 Polling 모드로 전환한다.
- 응답이 없으면 허용 Chat ID 설정이 빠졌는지 확인한다.

---

## 자주 막히는 지점

**설치 후 실행 명령이 먹히지 않을 때**  
설치 경로, PATH, 가상환경, 터미널 재시작 여부를 차례로 확인한다.

**API 키가 있어도 모델 호출이 실패할 때**  
키의 권한, 과금 상태, 환경변수 이름, 네트워크를 확인한다.

**처음부터 너무 큰 일을 맡겼을 때**  
"내 회사 전체 업무 자동화해줘"보다 "회의록을 요약하고 다음 할 일을 뽑아줘"가 훨씬 좋은 시작점이다.

---

## 과제

다음 양식으로 자신의 자동화 후보를 정리한다.

```markdown
# Week 1 과제: 나의 자동화 후보

## 자동화 후보 1
- 업무:
- 반복 빈도:
- 지금 불편한 점:
- Hermes Agent에게 맡길 첫 요청:
- 난이도 (쉬움/보통/어려움):

## 자동화 후보 2
- 업무:
- 반복 빈도:
- 지금 불편한 점:
- Hermes Agent에게 맡길 첫 요청:
- 난이도 (쉬움/보통/어려움):

## 자동화 후보 3
- 업무:
- 반복 빈도:
- 지금 불편한 점:
- Hermes Agent에게 맡길 첫 요청:
- 난이도 (쉬움/보통/어려움):

## 가장 먼저 시도할 후보
- 선택한 후보:
- 이유:
```

**제출 기준:**
- 자동화 후보 3개가 모두 채워져 있다.
- 각 후보에 반복 빈도와 불편한 점이 적혀 있다.
- 각 후보에 Hermes Agent에게 맡길 첫 요청 문장이 있다.
- 가장 먼저 시도할 후보 1개를 표시한다.

---

## 복습 질문

1. 일반 챗봇과 작업형 AI 에이전트의 차이는 무엇인가?
2. Hermes Agent에서 MEMORY.md, SOUL.md, AGENTS.md는 각각 어떤 역할을 하는가?
3. 클라우드 API와 로컬 LLM의 장단점은 무엇인가?
4. 처음 Hermes Agent에게 일을 맡길 때 요청을 작게 잡아야 하는 이유는 무엇인가?
5. 이번 주에 찾은 자동화 후보 중 가장 먼저 시도할 것은 무엇인가?

---

## 강사용 체크리스트

> ⚠️ **강의 전 반드시 점검하세요.**

- [ ] Windows, macOS, Linux별 설치 명령을 최신 WikiDocs 문서 기준으로 확인한다.
- [ ] API 키 환경변수 설정 예시를 최신 기준으로 확인한다.
- [ ] 네트워크/API 결제 문제가 생길 때 사용할 데모 계정, 녹화 화면, 읽기 전용 관찰 실습 중 하나를 준비한다.
- [ ] 설치에 실패한 수강생도 Lab 2와 과제는 진행할 수 있게 샘플 대화 결과를 제공한다.
- [ ] 지원되는 슬래시 명령어 목록을 최신 버전으로 확인한다.
- [ ] 텔레그램 게이트웨이 활성화 명령과 설정 파일 키 이름을 최신 문서로 확인한다.
- [ ] Lab 3 실습 전에 강의 환경에서 BotFather 봇 생성 → Token 발급 → 연동까지 사전 테스트를 완료한다.
- [ ] 수강생이 개인 Bot Token을 노출하지 않도록 `.env` 파일과 `.gitignore` 사용법을 안내한다.

---

## 다음 주 예고

다음 주에는 Hermes Agent를 **내 프로젝트에 맞게 길들이는 방법**을 배운다.  
도구 시스템, 메모리, 프로젝트 지침(AGENTS.md), 스킬을 연결해 "항상 같은 방식으로 반복되는 업무"를 작은 하네스로 바꾼다.
